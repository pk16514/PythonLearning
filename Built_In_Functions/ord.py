"""
   1. The syntax of ord() is: ord(ch)

   2. The ord() function takes a single parameter:
      --> ch - a Unicode character

   3. The ord() function returns an integer representing the Unicode character.

   4. ASCII only encodes 128 characters, the current Unicode has more than
      100,000 characters from hundreds of scripts.
"""
# Example-1


print(ord('5'))
print(ord('A'))
print(ord('$'))
